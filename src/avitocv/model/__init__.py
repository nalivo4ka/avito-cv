"""Модель классификатора ориентации и всё, что нужно для предсказания.

    architecture  архитектуры и подсчёт их стоимости в параметрах и умножениях
    inference     симметризация предсказаний и калибровка температурой

Два решения определяют форму решения целиком.

**Форма сети идёт от природы сигнала.** Ориентацию выдаёт вертикальная асимметрия строки:
выносные элементы букв, положение базовой линии, высота точек. Поэтому высота сворачивается
в каналы (иначе «где по вертикали» потерялось бы при усреднении), а агрегируется только ширина
(каждая буква даёт независимое свидетельство, и их надо сложить).

**Симметрия задачи навязывается, а не выучивается.** Для любого кропа ровно одна из ориентаций
верна, значит p(x) + p(rot180(x)) = 1 тождественно. `SymmetricPredictor` делает это соотношение
точным ценой двойного прогона, что заодно даёт бесплатную диагностику на тестовой выборке:
отклонение от единицы измеримо без меток.
"""
