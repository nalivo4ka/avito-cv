"""Чтение кропов из фотодатасетов: разметка превращается в манифест.

Метка ориентации достаётся бесплатно из самой разметки. И HierText, и TextOCR задают
четырёхугольник вокруг текста, у которого нулевая вершина — левый верхний угол **текста**,
а не бокса. Значит угол верхнего ребра сам говорит, ровная строка в кадре или перевёрнутая,
и распознаватель не нужен.

`hiertext` читает строчную разметку, `textocr` — словную и склеивает соседние слова в строки,
`crop_datasets` готовит кириллические наборы для валидации. Все три отдают `CropRecord`,
из которых собирается манифест (`data.manifest`) — путь к снимку плюс координаты бокса,
без копирования самих фотографий.
"""
