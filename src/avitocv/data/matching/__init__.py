"""Подгонка выборок под тестовую: профиль, измерение, выравнивание геометрии.

Тест выдан без меток, но его геометрию измерить можно, и это не утечка: поворот на 180°
не меняет ни ширину кропа, ни высоту, поэтому из распределения размеров метку извлечь
нельзя даже в принципе.

`profiling` измеряет кропы, `profile` хранит получившийся паспорт выборки, `geometry`
приводит к нему чужую выборку — весами для метрики и пересэмплингом для обучения.
"""
