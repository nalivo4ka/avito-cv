"""Отрисовка кропа с нуля: от строки текста до готовой картинки.

Порядок такой. `writing_systems` определяет, какими символами вообще можно писать, `fonts`
отбирает шрифты, умеющие их нарисовать, `corpus` даёт сам текст, `palettes` — цвета переднего
плана и фона с проверкой контраста. `rendering` превращает строку в слой с глифами,
`backgrounds` готовит подложку, `composition` кладёт строку на подложку так, чтобы получился
кроп нужных пропорций — с наклоном, соседними строками сверху и снизу, полями.

Здесь нет ни поворота, ни метки: модуль отдаёт **ровный** кроп. Поворот и всё, что от него
зависит, живёт уровнем выше, в `data.datasets`, — так проще не нарушить главный инвариант
пайплайна (см. `data.__init__`).
"""
