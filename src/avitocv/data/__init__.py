"""Сборка обучающих данных для классификации ориентации текстового кропа 0°/180°.

Метка здесь получается бесплатно: заведомо ровная строка — это класс 0, она же, повёрнутая
на 180°, — класс 1. Поэтому весь пакет решает ровно одну задачу: произвести **заведомо ровный**
кроп, статистически неотличимый от бокса, который выдаёт детектор текста на картинках Авито.

Конвейер одного обучающего примера
----------------------------------

    synthesis.corpus.TextSource            что написать -> строка текста
    synthesis.fonts.FontRegistry           чем написать -> шрифт под нужную письменность
    synthesis.palettes.ColorSchemeSampler  каким цветом -> фон, цвет текста, обводка
    synthesis.rendering.TextLineRenderer                -> GlyphLayer: RGBA-слой с чернилами
    synthesis.backgrounds.BackgroundProvider            -> подложка нужного размера
    synthesis.composition.CropComposer                  -> отступы, соседи, окно обрезки
    degradation.build_capture_stage()                   -> перспектива, размытие, яркость
    degradation.ResizeToHeight                          -> высота из профиля тестовой выборки
    --------------------------- граница поворота ---------------------------
    datasets.OrientationDataset                         -> поворот на 180° согласно метке
    degradation.build_codec_stage(work_height)          -> джиттер разрешения, шум, JPEG
    datasets.ImagePreprocessor                          -> нормированный тензор (C, H, W)

`work_height` — вдвое выше входа сети (при входе 48 это 96). Деградации применяются на нём, а
не на нативном разрешении кропа: всё, что мельче двух пикселей на пиксель входа, после ужатия
неразличимо, а на крупном кропе полное разрешение стоило в сорок раз дороже.

Всё, что выше границы поворота, симметрично относительно поворота на 180°, поэтому применяется
к ещё ровному кропу. Всё несимметричное — прежде всего сетка блоков JPEG, которая всегда идёт
от левого верхнего угла, — применяется строго после поворота. Иначе класс можно было бы угадать
по артефактам сжатия, а не по тексту.

Шаги до границы поворота реализует `sources.TextLineSource`, шаги после — `datasets`.
Благодаря этому разметка не зависит от происхождения кропа: синтетика, заранее сгенерированное
хранилище и вырезки из реальных фотографий по манифесту подставляются в один и тот же датасет.
Поэтому же подпакеты `synthesis` и `real` ничего не знают ни о повороте, ни о метке — оба
отдают только ровный кроп.

Модули по ролям
---------------

Общие для всех источников кропа:

    sampling     детерминированный рандом: сид по индексу сэмпла, диапазоны, взвешенный выбор
    sources      поставщики ровных кропов: синтетика, хранилище, манифест, пересэмплинг
    datasets     поворот, метка, препроцессинг в тензор, смешивание источников
    degradation  деградации изображения, разложенные на съёмочную и кодековую стадии
    storage      шардовое хранилище заранее сгенерированных кропов и индекс к нему
    manifest     манифест реальных кропов: путь к снимку и координаты бокса
    factory      сборка всего перечисленного по конфигу

Подпакеты (у каждого своя докстрока с подробностями):

    synthesis    отрисовка кропа с нуля: письменности, шрифты, корпуса, палитры, рендер,
                 фоны, компоновка
    real         чтение кропов из фотодатасетов: HierText, TextOCR, кириллические наборы
    matching     подгонка выборок под тестовую: измерение, профиль, выравнивание геометрии

Точка входа
-----------

    from avitocv.config import DataConfig
    from avitocv.data.factory import SyntheticDatasetFactory

    factory = SyntheticDatasetFactory(DataConfig.from_yaml("configs/data.yaml"))
    dataset = factory.build_training()
    tensor, label = dataset[0]
"""
